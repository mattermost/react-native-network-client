import React from "react";
import { View, Text } from "react-native";

const APIClientUploadScreen = ({}: APIClientUploadScreenProps) => {
    return (
        <View>
            <Text>Upload</Text>
        </View>
    );
};

export default APIClientUploadScreen;
